spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: "@project.name@"
    description: "@project.description@"
    version: "@project.version@"

hazelcast:
  declaration-map:
    init: true
    size: 1_000_000

logging.level:
  ru.parma.ventslavovich.hazelcast: debug